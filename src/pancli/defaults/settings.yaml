theme:
  mode: auto

ui:
  show_overview: true

transfer:
  default_jobs: 4
  chunk_size: 65536
  refresh_per_second: 6
  ema_alpha: 0.25

search:
  default_depth: 3
  max_depth: 10

network:
  connect_timeout: 5
  read_timeout: 30
  request_timeout: 30
  max_retries: 3
  retry_backoff: 1.5
